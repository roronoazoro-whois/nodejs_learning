{
  "name": "bai05",
  "version": "1.0.0",
  "description": "- npm init: Lệnh npm init được sử dụng để khởi tạo một dự án Node.js mới bằng cách tạo một file package.json. File này chứa thông tin về dự án, bao gồm tên, phiên bản, mô tả, các phụ thuộc (dependencies), và các script.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --inspect index.js",
    "watch": "node-sass --watch scss/ --output public/css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "express-handlebars": "^8.0.1",
    "mongoose": "^8.6.0",
    "mongoose-slug-updater": "^3.3.0"
  },
  "devDependencies": {
    "husky": "^9.1.5",
    "lint-staged": "^15.2.9",
    "morgan": "^1.10.0",
    "node-sass": "^9.0.0",
    "prettier": "^3.3.3"
  }
}
